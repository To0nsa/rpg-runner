description: Accepted replay handoffs whose immediate settlement request failed and used durable fallback delivery.
filter: |-
  resource.type="cloud_run_revision"
  resource.labels.service_name="replay-validator"
  jsonPayload.event="replay_validator.dispatch"
  jsonPayload.phase="settlement_dispatch_fallback"
  jsonPayload.status="accepted"
metricDescriptor:
  metricKind: DELTA
  valueType: INT64
  unit: "1"
