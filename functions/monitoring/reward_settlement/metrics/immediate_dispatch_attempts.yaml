description: Accepted replay handoffs that started the immediate settlement request.
filter: |-
  resource.type="cloud_run_revision"
  resource.labels.service_name="replay-validator"
  jsonPayload.event="replay_validator.dispatch"
  jsonPayload.phase="settlement_dispatch_start"
  jsonPayload.status="accepted"
metricDescriptor:
  metricKind: DELTA
  valueType: INT64
  unit: "1"
