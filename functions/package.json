{
  "name": "rpg-runner-functions",
  "private": true,
  "engines": {
    "node": "24"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "build:watch": "tsc --watch",
    "serve": "corepack pnpm run build && firebase emulators:start --only functions,firestore",
    "shell": "corepack pnpm run build && firebase functions:shell",
    "deploy": "corepack pnpm run build && firebase deploy --only functions,firestore:rules,firestore:indexes",
    "test": "corepack pnpm run build:test && firebase emulators:exec --config ../firebase.test.json --project demo-rpg-runner-functions-tests --only firestore \"corepack pnpm run test:compiled\"",
    "test:compiled": "node --test \"lib_test/test/**/*.test.js\""
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0",
    "@google-cloud/tasks": "^6.2.3",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "@types/node": "^24.13.3",
    "firebase": "12.0.0",
    "firebase-tools": "15.2.1",
    "typescript": "^5.9.3"
  }
}
