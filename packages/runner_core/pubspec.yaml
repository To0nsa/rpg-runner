name: runner_core
description: Deterministic gameplay core for RPG Runner.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  crypto: ^3.0.7

dev_dependencies:
  test: ^1.25.8
  vm_service: ^15.0.2
