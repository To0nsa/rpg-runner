/// Public barrel for the deterministic runner core package.
///
/// Consumers may import leaf libraries directly from `package:runner_core/...`
/// when they need specific contracts.
library;

export 'game_core.dart';
