/// Stable identifiers for one-shot spell impact visuals.
///
/// These IDs are used by Core events and render registries to map authored
/// impact effects without coupling gameplay systems to asset paths.
enum SpellImpactId {
  unknown,
  fireExplosion,
}
