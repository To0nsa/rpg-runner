# Projection Cost Optimization Rollout — 2026-09-01

## Status

Production deployment completed on September 1, 2026. The natural cursor-cycle
and 24-hour idle observation remain open, so this building plan must not be
archived yet.

Project and operator:

- Google Cloud project: `rpg-runner-d7add`
- Region: `europe-west1`
- Authorized operator account: `lothringen.rpg@gmail.com`
- Rollout window: `2026-09-01T19:12:28Z` through `2026-09-01T19:36:22Z`

Implementation milestones:

- plan: `46af0962`
- validator no-op materialization: `512fa5b6`
- bounded Functions reconciliation: `bfa0c257`
- logging and monitoring guardrails: `29ffb6ad`
- local validation record: `63bae769`

## Read-only baseline

The baseline inventory at `2026-09-01T19:12:28Z` found:

- 54 retained leaderboard boards;
- 5 player-best documents;
- 5 ghost manifests;
- 54 materialized Top-10 views;
- 42 run sessions and 25 accepted validated runs;
- no invalid player-best or ghost source references.

Live configuration before rollout:

- validator revision: `replay-validator-00035-8vn`;
- validator image digest:
  `sha256:094c2b4009ea25c5baf2e737a98a00364a887a7c19ed1cb585e66a2b628c3580`;
- reconciliation Function last update: `2026-08-01T19:01:52Z`;
- Scheduler cadence: `every 15 minutes`;
- validation queue operation-log sampling: `1.0`;
- projection queue operation-log sampling: `1.0`.

## Validator deployment

Cloud Build `9441552a-f83f-4ea6-8f32-61573e03cce3` built and pushed:

- tag: `replay-validator:cost-opt-20260901-221252`;
- immutable digest:
  `sha256:fd491dc3bb44337b3111e2f15668896c2b28c37ef73649dab563a361978bf590`.

Revision `replay-validator-00036-9f5` began serving 100 percent of traffic. Its
effective configuration is minimum instances 0, maximum instances 10,
concurrency 1, 1 CPU, 512 MiB memory, and a 240-second request timeout. Both
`/live` and `/ready` returned success after deployment.

Rollback image:

`europe-west1-docker.pkg.dev/rpg-runner-d7add/replay/replay-validator@sha256:094c2b4009ea25c5baf2e737a98a00364a887a7c19ed1cb585e66a2b628c3580`

## Validator canary evidence

Existing unreleased-game boards were used for the no-op checks, avoiding fake
player records:

- empty board hash `8b302922f7fae0bd`: the second reconciliation left the
  Top-10 update time unchanged at `2026-09-01T19:17:02.794926Z`;
- populated board hash `6e0a5b22e83ef2b4`: the second reconciliation left the
  view, player-best, and ghost-manifest update times unchanged;
- both views converged to materialization schema 1 with a 64-character
  materialized revision.

An isolated ghost-only canary used one temporary board, player-best document,
validated-prefix source object, and promoted ghost object. Board hash
`56b680bb77eb0210` produced these structured phases:

1. `projection_reconciliation_ghost_only_change` at
   `2026-09-01T19:25:30.464687Z`;
2. `projection_reconciliation_unchanged` at
   `2026-09-01T19:25:30.866526Z`.

The second delivery left the view, player-best, and manifest update times
unchanged. The temporary board, player-best, view, manifest, validated-prefix
source object, and ghost object were then deleted and verified absent.

An initial version of that canary used a source prefix outside the validator
service account's read condition. It returned a retryable 503
with `ghost_DetailedApiRequestError_http_403_forbidden`, proving the existing
least-privilege boundary. Board hash `55d45227200ebdc8` and both of its storage
objects were also verified cleaned.

The repository's anonymous end-to-end canary was not used as acceptance
evidence: production correctly rejected it because a linked Google Play Games
identity is required. Its isolated Auth user hash `37aefd9a530c2402` was deleted
through the Identity Toolkit administrative API and verified absent. No game
documents were created by that attempt. A real disposable Play Games identity
is still required for the Phase 8 normal-path canary.

## Functions and Scheduler deployment

Firebase deployed source hash `7d613c2ea01a137f959398fecfe629b6e39a58d9`
from the committed implementation. Cloud Functions build
`48f1f45c-b64c-4aff-9505-31cfd13ff1de` produced revision
`runprojectionreconciliation-00005-riq`.

Effective live state:

- Scheduler cadence: `every 60 minutes`, UTC;
- default/effective reconciliation batch: 4;
- Function memory: 512 MiB;
- Function minimum instances: 0;
- all 26 deployed Functions have minimum instances 0.

Two manual Scheduler invocations verified consecutive pages:

| UTC log time | Queried | Selected | Enqueued | Cursor committed | Cursor hash |
| --- | ---: | ---: | ---: | --- | --- |
| `2026-09-01T19:30:34.723658Z` | 5 | 4 | 4 | yes | `a07d8056f8f3b616` |
| `2026-09-01T19:31:55.885716Z` | 5 | 4 | 4 | yes | `1d0b8b8ae8b9ced1` |

Both pages reported `effectiveBatchSize: 4` and `retainedBoardCount: 54`.
The fifth document was lookahead only, the cursor advanced between pages, and
the projection queue drained to zero after each invocation.
Conditional cursor conflicts and exact-multiple wrapping remain covered by the
33 focused projection tests recorded before deployment.

Prior Function rollback reference:

- revision: `runprojectionreconciliation-00004-yup`;
- prior cadence: `every 15 minutes`;
- prior committed source is the parent state before milestone `bfa0c257`.

## Logging, monitoring, and budget guardrails

Final queue state:

- `replay-validation`: running, empty, operation-log sampling `1.0`;
- `replay-projection`: running, empty, operation-log sampling `0.1`.

Monitoring application completed successfully:

- all 9 checked-in replay projection/validator log metrics exist;
- all 11 checked-in policies were created or updated and use the verified
  `RPG Runner production alerts` email channel;
- dashboard `RPG Runner - Projection cost and recovery` exists as
  `0b951b3c-d60d-428d-b00f-9c488c08051d`;
- the retained-board release-limit alert is enabled;
- two existing EUR 1 monthly project budgets remain active.

## Post-rollout inventory

The read-only inventory at `2026-09-01T19:36:22Z` again found 54 boards, 5
player bests, 5 ghost manifests, 54 views, 42 run sessions, 25 accepted
validated runs, and zero invalid projection sources. This matches the baseline
and confirms no canary residue.

## Remaining observation gates

Do not archive this plan until all of the following are recorded:

- a supported disposable run using a real linked Google Play Games identity;
- one natural complete cursor cycle (54 boards at four per hour requires 14
  healthy scheduled invocations);
- a representative 24-hour no-player window;
- no more than 96 healthy scheduled projection requests in that window;
- projection-related reads below 1,000 for the recorded release corpus;
- zero unchanged materialization writes after initial convergence;
- healthy retry counts, oldest-task age, and billing by service/SKU.
